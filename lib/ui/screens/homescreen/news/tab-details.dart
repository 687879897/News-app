import 'package:flutter/material.dart';

class TabDateils extends StatelessWidget {
  const TabDateils({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
